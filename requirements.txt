# Pinned to the versions verified working in the local env.
# Upper bounds loose — tighten if a future minor breaks the app.
streamlit==1.56.0
pandas==2.1.4
numpy==1.23.5
plotly==6.7.0
requests==2.32.4
urllib3==1.26.14
